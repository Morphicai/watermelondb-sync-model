/**
 * 同步模块常量定义
 * 
 * @description
 * 存放同步系统使用的全局常量
 */

/**
 * 默认软删除字段名
 * 
 * @description
 * 当模型未指定 softDeleteField 时使用的默认字段名。
 * 用于标记记录是否已被软删除（逻辑删除而非物理删除）。
 */
export const DEFAULT_SOFT_DELETE_FIELD = 'is_deleted';

